import type { UnifiedInboundMessage } from '../../../shared/types/unified-inbound-message';

export function normalizeInboundMessage(message: UnifiedInboundMessage): UnifiedInboundMessage {
  return message;
}
